#!/bin/bash

echo "Type the branch name"

read branch

git checkout -b ${branch}

sleep 1

git init .

sleep 1

echo "Type the name of a file"

read FN

git add .

sleep 1

echo "Type the commit text"

read commit

git commit -m "${commit}"

sleep 1

git push --set-upstream origin ${branch}
